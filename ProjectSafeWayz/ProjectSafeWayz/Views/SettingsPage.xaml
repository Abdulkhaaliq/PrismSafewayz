<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="http://prismlibrary.com"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="ProjectSafeWayz.Views.SettingsPage">
    <ContentPage.Content>
          <StackLayout Margin="20,30">
            <Label Text="Settings" TextColor="Blue" FontSize="Large" HorizontalTextAlignment="Center"/>
            <Button Text="Notifications" Command="{Binding NotificationCommand}" VerticalOptions="Center"/>
            <Button Text="Filter" Command="{Binding FilterCommand}" VerticalOptions="Center"/>
            <Button Text="Account" Command="{Binding AccountCommand}" VerticalOptions="Center"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>